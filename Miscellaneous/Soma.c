#include <stdio.h>

int main() {
    int x, y;
    printf("Digite um valor: ");
    scanf("%d", &x);
    printf("Digite outro valor: ");
    scanf("%d", &y);
    printf("%d", x + y);
    return(0);
}